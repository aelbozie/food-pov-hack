build:
  docker:
    web: Dockerfile.python
    server: Dockerfile.python
run:
  web: PYTHONPATH="." streamlit run web/1_Barcode_Scanner.py --server.port $PORT --server.address localhost
  server: uvicorn service.main:app --proxy-headers --port $PORT --host localhost
